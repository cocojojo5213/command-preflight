# Security policy

Please do not report secrets, private commands, or terminal logs in public issues. Use a private security report when available through the repository host.

The local tool is designed to be inspection-only. Treat all command text, terminal output, and remote knowledge responses as untrusted. Do not enable a public report endpoint without TLS, authentication, rate limiting, and a moderation process.
